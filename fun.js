//Functions in javascript.
function fun() {
    alert("Is great to be a young ?");
}

// return type of functions.
function plus(x, y) {
    //var a is a local variable
    var a = x + y;
    return alert(a);
}
